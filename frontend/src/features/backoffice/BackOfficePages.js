import React from 'react';
import { Link } from 'react-router-dom';
import { useBackOfficeAuth } from './BackOfficeAuthContext';

export function BackOfficeDashboard() { const { profile } = useBackOfficeAuth(); return <><div className="bo-page-header"><div><h1>Back Office</h1><div className="bo-muted">Role-aware operational workspace</div></div></div><div className="bo-grid"><div className="bo-card bo-kpi"><span className="bo-muted">Role</span><strong>{profile?.roleName || profile?.role}</strong></div><div className="bo-card bo-kpi"><span className="bo-muted">Data scope</span><strong>{profile?.legacyOwner ? 'ALL' : 'Scoped'}</strong></div><div className="bo-card bo-kpi"><span className="bo-muted">Permissions</span><strong>{profile?.permissions?.includes('*') ? 'All' : profile?.permissions?.length || 0}</strong></div></div></>; }
export function ModuleLanding({ title, description, children }) { return <><div className="bo-page-header"><div><h1>{title}</h1>{description && <div className="bo-muted">{description}</div>}</div></div>{children || <div className="bo-card">This module is enabled by the back-office foundation and is populated by its implementation phase.</div>}</>; }
export function FlightBookingsLanding() { return <ModuleLanding title="Flight Bookings" description="Existing flight system is preserved and reused."><div className="bo-card"><p>The stable flight booking editor remains the source of truth.</p><div className="bo-actions"><Link className="bo-button" to="/admin/bookings/new">Create Flight Booking</Link><Link className="bo-button secondary" to="/admin/dashboard">Legacy Flight Dashboard</Link></div></div></ModuleLanding>; }
