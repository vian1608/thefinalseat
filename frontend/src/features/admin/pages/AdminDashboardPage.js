// The legacy dashboard accumulated multiple overlapping save/delete/email handlers.
// Keep the public route/import stable while using the rebuilt, contract-verified dashboard.
export { default } from './AdminDashboardPageV2';
