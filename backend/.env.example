# Server Configuration
NODE_ENV=development
PORT=5001
FRONTEND_URL=http://localhost:3000

# ─── Supabase ────────────────────────────────────────────────
SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key
SUPABASE_SECRET_KEY=your_supabase_secret_key

# ─── Email — Resend ───────────────────────────────────────────
RESEND_API_KEY=your_resend_api_key
RESEND_FROM=The Final Seat LLC <onboarding@resend.dev>

# ─── Stripe Payment Gateway ──────────────────────────────────
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
STRIPE_MOCK_MODE=true

# ─── PayPal Payment Gateway ──────────────────────────────────
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here
PAYPAL_ENV=sandbox
PAYPAL_WEBHOOK_ID=your_paypal_webhook_id_here

# ─── SerpAPI ─────────────────────────────────────────────────
SERPAPI_API_KEY=your_serpapi_api_key

# ─── VGS Secure Payment Vault ────────────────────────────────
# Service-account credentials stay SERVER-SIDE only. Grant aliases:write and aliases:delete.
VGS_VAULT_ID=your_vgs_vault_id
VGS_ENVIRONMENT=sandbox
VGS_CLIENT_ID=your_vgs_service_account_client_id
VGS_CLIENT_SECRET=your_vgs_service_account_client_secret
# Optional when the vault contains multiple inbound routes / custom hostnames.
VGS_COLLECT_ROUTE_ID=
VGS_COLLECT_CNAME=
VGS_SHOW_CNAME=
# The application is designed for a 24-hour TARGET volatile CVV window.
# Do not set VGS_CVV_TTL_CONFIRMED=true until VGS has actually configured/confirmed it for this vault.
VGS_CVV_TTL_HOURS=24
VGS_CVV_TTL_CONFIRMED=false
SECURE_PAYMENT_PUBLIC_LINK_HOURS=48
SECURE_PAYMENT_OTP_MINUTES=5
SECURE_PAYMENT_ACCESS_MINUTES=5

# ─── Admin ───────────────────────────────────────────────────
ADMIN_EMAIL=admin@thefinalseat.com
ADMIN_PASSWORD=your_admin_password

# ─── JWT ─────────────────────────────────────────────────────
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d
